# steamsmurfinvestigator

# Running the server
Using VSCode, run the Python FastAPI configuration to run the server, debug the code, and have hot reload.
The secrets.json is in the development discord.
